{
    "abi": [
    "function execute(address dest, uint256 value, bytes data) external payable returns (bool, bytes)",
    "function executeBatch(address[] calldata dest, bytes[] calldata func) external"
  ]
}